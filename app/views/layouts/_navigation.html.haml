%nav.navbar.navbar-inverse.navbar-default{:role => "navigation"}
  .container-fluid
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => "#{root_path}"} SPI
    / Collect the nav links, forms, and other content for toggling
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li.active
          %a{:href => "#{root_path}"} Home
      %form.navbar-form.navbar-right{:role => "jump"}
        .form-group
          = form_tag "/days/display/", method: :get do
            = select_tag 'lecture', options_from_collection_for_select(@days, :id, :lecture, @day.id), :onchange => "this.form.submit();", class: 'form-control'
    / /.navbar-collapse
  / /.container-fluid

%ul.nav
  %li
    
  %li
    